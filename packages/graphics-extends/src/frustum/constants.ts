import { Color } from 'cesium';

export const DEFAULT_FRUSTUM_OPTIONS = {
  show: true,
  fov: 60,
  near: 1,
  far: 1000,
  aspectRatio: 1.0,
  fill: true,
  fillColor: Color.RED,
  fillOpacity: 0.3,
  outline: true,
  outlineColor: Color.WHITE,
};
